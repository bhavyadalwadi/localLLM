# Backups

Use this directory for local backup notes and any temporary restore staging.

Suggested layout:
- `local/` for local snapshots or dry-run restores
- `nas/` for NAS mount notes or helper files

