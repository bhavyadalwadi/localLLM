# Staging node example
#
# This is intended for the current download/verification machine.
# It assumes:
# - Ollama runs on the host
# - Open WebUI may be started later against host Ollama
# - backups are written to a local path first

OPEN_WEBUI_PORT=3000
OLLAMA_PORT=11434
OLLAMA_BASE_URL=http://host.docker.internal:11434
OPEN_WEBUI_IMAGE=ghcr.io/open-webui/open-webui:main
OLLAMA_IMAGE=ollama/ollama:latest

# Replace before exposing outside a trusted local environment.
WEBUI_SECRET_KEY=change-me

# Use a local backup directory on the staging machine by default.
BACKUP_ROOT_DIR=$HOME/local-ai-node/backups
OLLAMA_HOME_DIR=$HOME/.ollama

# Optional override for the connectivity script.
OPEN_WEBUI_URL=http://localhost:3000

